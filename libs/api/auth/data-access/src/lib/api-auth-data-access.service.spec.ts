import { apiAuthDataAccess } from './api-auth-data-access.service';

describe('apiAuthDataAccess', () => {
  it('should work', () => {
    expect(apiAuthDataAccess()).toEqual('api-auth-data-access');
  });
});
