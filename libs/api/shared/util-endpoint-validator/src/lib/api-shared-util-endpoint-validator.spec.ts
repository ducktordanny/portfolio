import {apiSharedUtilEndpointValidator} from './api-shared-util-endpoint-validator';

describe('apiSharedUtilEndpointValidator', () => {
  it('should work', () => {
    expect(apiSharedUtilEndpointValidator()).toEqual('api-shared-util-endpoint-validator');
  });
});
